# Execution profile — opentimbre

> Detected by `pelizzai-audit` at bootstrap (2026-08-03) from the repository's real state.
> This project is **greenfield**: it has no manifest, no lockfile, and no scripts yet, so the
> *Commands*, *Package manager*, and *Stack baseline* sections record that absence honestly instead
> of guessing. Fill them from the real manifest as soon as the project is scaffolded.

## Harness and skill roots

- Source mode: `false`
- Canonical skill root: `.claude/skills`
- Installed mirrors: `.agents/skills` (generated by `scripts/sync-harness.mjs`; must stay
  byte-for-byte). `.cursor/rules/` is a manual adapter, not generated.

## Ratified execution defaults

> PROJECT policy explicitly ratified by the user — NOT inheritance from the previous task.
> All `<unset>` at bootstrap; the post-plan gate is where the user ratifies them.

- isolation-default: <unset>
- execution-mode-default: <unset>
- commit-strategy-default: <unset>
- review-policy-default: <unset>
- Ratified on: <unset> | Overrides since then: 0
<!-- destination is not persistable: push/PR/publish require per-task confirmation -->

## Commands

_No scripts exist yet — the project has no `package.json`. Rows are added from the real manifest
once it exists; nothing here is inferred from the legacy project._

| Action | Exact command | Directory |
| ------ | ------------- | --------- |
| _(none yet)_ | | |

## Package manager

- Manager: `<undetermined>` — no lockfile in this repository yet.
- Reference only: the legacy project used **npm** (`package-lock.json`). That does not bind the
  rebuild; the choice is ratified with the stack.

## Stack baseline

_Snapshot date: 2026-08-03 (bootstrap). **No manifest exists yet**, so this table records the
TARGET stack named by the user plus the versions currently published, verified against the npm
registry on that date. It is not yet a drift anchor — replace the "current published" column with
the manifest-pinned versions once the project is scaffolded._

| Technology | Version (manifest) | Current published (2026-08-03) |
| ---------- | ------------------ | ------------------------------ |
| Angular | `<not pinned yet>` | 22.1.0 |
| Electron | `<not pinned yet>` | 43.2.0 (`engines.node >= 22.12.0`) |
| Node | `<not pinned yet>` | — (Electron 43 bundles its own) |
| electron-builder | `<not chosen yet>` | 26.15.3 |
| @julusian/midi | `<not pinned yet>` | 3.8.0 (N-API 7 prebuilds) |

_Anchor of the version-driven and adoption-driven axes of `pelizzai-writing-skills`._

## Available MCPs

- `pencil` — .pen design file editor. Available.
- `context7` — **NOT available in this session.** This is the harness's preferred technical source;
  without it, stack skills were grounded in current official documentation and the npm registry,
  and that limitation is recorded per skill in the ledger. Recommend installing it.
- `claude.ai Google Drive`, `claude.ai Notion` — present but **unauthorized**; they need
  authorization through claude.ai connector settings before their tools can be used.

## Legacy reference

The pre-existing implementation lives in `legacy/` — a separate Git repository, excluded from this
one by the root `.gitignore` (`/legacy/*`). It is the behavioral reference for the rebuild.

Note for anyone reading it: the legacy working tree has its **root files deleted but uncommitted**
(`package.json`, `tsconfig.json`, `README.md`, `capabilities.md`, `padroes.md`, `spec-poc-gojira.md`).
They are intact in its `HEAD` — read them with `git show HEAD:<file>` from inside `legacy/`, never
conclude a root-level file "doesn't exist" from a missing-on-disk check alone. **Incident
(2026-08-04, Task 6):** a subagent porting the Gojira descriptor concluded `capabilities.md` was
absent from disk and substituted an unauthorized, unverified sibling repo
(`neural-ai-control/capabilities.md`) as its cross-check source instead. The transcribed data
turned out correct when independently re-verified against the real `git show HEAD:capabilities.md`,
but the docstring shipped a false provenance claim, caught only at review. Any task touching
`legacy/` root files must check `git show HEAD:<file>` before concluding one is missing, and must
never reference a directory outside `c:\Users\dingo\github\opentimbre` without explicit authorization.
